---
---
<section class="relative z-10 w-full border-y border-white/5 bg-[#0a0718] overflow-hidden py-24">
  <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-full max-w-3xl h-80 bg-purple-600/5 blur-[120px] rounded-full pointer-events-none"></div>

  <div class="max-w-3xl mx-auto px-6 text-center relative z-10 animate-fade-up opacity-0 delay-300">

    <p class="font-mono text-purple-300 text-xs tracking-widest uppercase mb-6 flex items-center justify-center gap-3">
      <span class="w-4 h-px bg-purple-500/50"></span>
      The solution
      <span class="w-4 h-px bg-purple-500/50"></span>
    </p>

    <h2 class="font-display text-4xl md:text-6xl font-bold text-white tracking-tighter mb-6 leading-[1.1]">
      Not every request needs<br/>the same model.
    </h2>

    <p class="text-lg text-gray-400 font-light leading-relaxed max-w-xl mx-auto mb-10">
      Some tasks demand maximum reasoning. Others just need a fast, cheap response. Nexus makes that decision automatically — so you don't have to.
    </p>

    <!-- Highlighted statement -->
    <div class="inline-block border border-purple-500/20 bg-purple-500/5 px-6 py-4 rounded-sm font-mono text-sm text-purple-200">
      Match the right model to the right task. Automatically. Every time.
    </div>
  </div>
</section>
